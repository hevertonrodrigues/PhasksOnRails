<%= form_for(@tasks_status, :html_options => { :class => 'form' }) do |f| %>
  <% if @tasks_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tasks_status.errors.count, "error") %> prohibited this tasks_status from being saved:</h2>

      <ul>
      <% @tasks_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => 'form-control' %>
  </div>
  <div class="form-group actions">
    <%= f.submit :class => 'btn btn-success' %>
  </div>
<% end %>