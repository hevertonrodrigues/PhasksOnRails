<div class="page-header">
  <h1><%= t('site.editing_tasks_status') %></h1>
</div>
<%= breadcrumb %>

<%= render 'form' %>

<%= link_to '<i class="fa fa-search"></i> '.html_safe + t("site.show"), @tasks_status, { :class => 'btn btn-primary' } %> 
<%= back_link tasks_statuses_path %>
