<p id="notice"><%= notice %></p>

<p>
  <strong>Role:</strong>
  <%= @role.role %>
</p>

<%= link_to t('site.edit'), edit_role_path(@role), { :class => 'btn btn-success' } %> 
<%= back_link roles_path %>
