<div class="page-header">
  <h1><%= t('site.editing_color') %></h1>
</div>
<%= breadcrumb %>

<%= render 'form' %>

<%= link_to '<i class="fa fa-search"></i> '.html_safe + t("site.show"), @color, { :class => 'btn btn-primary' } %> 
<%= back_link colors_path %>
