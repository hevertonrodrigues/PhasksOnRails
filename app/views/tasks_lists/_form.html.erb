<%= form_for(@tasks_list, :html_options => { :class => 'form' }) do |f| %>
  <% if @tasks_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tasks_list.errors.count, "error") %> prohibited this tasks_list from being saved:</h2>

      <ul>
      <% @tasks_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, :class => 'form-control' %>
  </div>
  <div class="form-group actions">
    <%= f.submit :class => 'btn btn-success' %>
  </div>
<% end %>