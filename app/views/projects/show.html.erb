<div class="page-header">
  <h1><%= t('site.show_project') %></h1>
</div>
<%= breadcrumb %>

<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<p>
  <strong>Color:</strong>
  <%= @project.color.name %>
</p>

<%= link_to t('site.edit'), edit_project_path(@project), { :class => 'btn btn-success' } %> 
<%= back_link projects_path %>
